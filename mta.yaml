_schema-version: "3.2"
ID: vat
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: vat-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: vat-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - vat.zip
      name: vat
      target-path: resources/
- name: vat
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: vat-destination-content
  type: com.sap.application.content
  requires:
  - name: vat-destination-service
    parameters:
      content-target: true
  - name: vat-repo-host
    parameters:
      service-key:
        name: vat-repo-host-key
  - name: uaa_vat
    parameters:
      service-key:
        name: uaa_vat-key
  parameters:
    content:
      instance:
        destinations:
        - Name: vaa_vat_repo_host
          ServiceInstanceName: vat-html5-service
          ServiceKeyName: vat-repo-host-key
          sap.cloud.service: vaa
        - Authentication: OAuth2UserTokenExchange
          Name: vaa_uaa_vat
          ServiceInstanceName: vat-xsuaa-service
          ServiceKeyName: uaa_vat-key
          sap.cloud.service: vaa
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
resources:
- name: vat-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: vat-html5-service
    service-plan: app-host
- name: vat-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      version: 1.0.0
    service: destination
    service-name: vat-destination-service
    service-plan: lite
- name: uaa_vat
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: vat-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
